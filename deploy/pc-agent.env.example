# Copy to .env on the personal PC or export these variables before starting the agent.
GATEWAY_URL=https://gateway.example.com
AGENT_TOKEN=replace-with-the-server-agent-token
AGENT_ID=personal-pc-1
AGENT_WORKSPACE_PATH=/absolute/path/to/local/workspace
AGENT_POLL_INTERVAL=3
AGENT_JOB_TIMEOUT=600
